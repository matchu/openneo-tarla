- if @sightings.blank?
  %h2 Tarla hasn't been spotted recently, that elusive girl. Have you seen her?
  = render :partial => 'form', :locals => {:sighting => Sighting.new}
- else
  %ul#sightings= render @sightings
- content_for :controls do
  = link_to 'Add a Tarla sighting', new_sighting_path, :class => 'new-sighting-button'
